import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:route1/index', 'Route1IndexView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
